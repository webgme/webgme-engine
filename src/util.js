// THIS is a FAKE file, so that the borwser side compiler would be able to incorporate a module that functions 
// differently on the server and client side.
// FIXME: it should be controlled from the optimizer configuration, but so far there were no proper config for this.
/*globals define*/
/*eslint-env node, browser*/

/**
 * @author kecso / https://github.com/kecso
 */

define(function () {
    'use strict';
    return null;
});